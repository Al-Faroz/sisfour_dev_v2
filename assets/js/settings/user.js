(() => {
'use strict';
const app=document.getElementById('settingsUserApp');if(!app)return;
const base=String(app.dataset.baseUrl||'').replace(/\/+$/,'');
const body=document.getElementById('userBody'),info=document.getElementById('userInfo'),alertBox=document.getElementById('userAlert');
const form=document.getElementById('formUser'),resetForm=document.getElementById('formResetPassword');
const modal=bootstrap.Modal.getOrCreateInstance(document.getElementById('modalUser'));
const resetModal=bootstrap.Modal.getOrCreateInstance(document.getElementById('modalResetPassword'));
let offset=0,total=0;const limit=30;const managedById=new Map();
const esc=v=>{const e=document.createElement('div');e.textContent=v??'';return e.innerHTML};
function show(m,t='danger'){alertBox.className=`alert alert-${t}`;alertBox.textContent=m}function hide(){alertBox.classList.add('d-none')}
function params(){const p=new URLSearchParams({format:'json',limit,offset});const s=document.getElementById('userSearch').value.trim(),r=document.getElementById('userRoleFilter').value,st=document.getElementById('userStatusFilter').value;if(s)p.set('search',s);if(r)p.set('role',r);if(st)p.set('status',st);return p}
async function load(){try{const r=await fetch(`${base}/settings/user?${params()}`,{headers:{Accept:'application/json','X-Requested-With':'XMLHttpRequest'}});const j=await r.json();if(!r.ok||j.status!=='success'){show(j.message||'Gagal memuat user.');return}hide();const d=j.data||{};total=Number(d.total||0);managedById.clear();body.innerHTML=(d.rows||[]).map(x=>{managedById.set(String(x.id),!!x.credential_managed);const sec=(x.secondary_roles||[]).map(y=>`<span class="badge bg-label-secondary me-1">${esc(y)}</span>`).join('')||'-';return `<tr><td><strong>${esc(x.username)}</strong>${x.credential_managed?'<br><small class="text-muted">dikelola Master</small>':''}</td><td>${esc(x.identity_label||'Tanpa relasi')}</td><td>${esc(x.role||'NULL')}</td><td>${sec}</td><td>${Number(x.status_aktif)===1?'<span class="badge bg-success">Aktif</span>':'<span class="badge bg-secondary">Nonaktif</span>'}</td><td>${Number(x.auth_version||0)}</td><td class="text-end"><button class="btn btn-sm btn-outline-primary btn-edit-user" data-id="${x.id}">Edit</button> <button class="btn btn-sm btn-outline-warning btn-reset-user" data-id="${x.id}">Reset</button>${x.credential_managed?'':` <button class="btn btn-sm btn-outline-danger btn-delete-user" data-id="${x.id}">Hapus</button>`}</td></tr>`}).join('')||'<tr><td colspan="7" class="text-center text-muted">Tidak ada data.</td></tr>';info.textContent=`${total?offset+1:0}-${Math.min(offset+limit,total)} dari ${total}`;document.getElementById('userPrev').disabled=offset<=0;document.getElementById('userNext').disabled=offset+limit>=total;bindRows()}catch(e){show('Terjadi kesalahan jaringan.')}}
function clearIdentity(){['id_guru','id_pegawai','id_siswa'].forEach(n=>form.elements[n].value='')}
function resetIdentity(){clearIdentity();document.getElementById('identityResult').innerHTML='<option value="">Pilih identitas</option>';document.getElementById('identityHelp').textContent=''}
function setManagedUI(managed){form.elements.username.readOnly=managed;document.getElementById('usernameHelp').textContent=managed?'Username otomatis mengikuti NIP/NIK dari Master.':'Username dapat diatur untuk akun non-Guru/Pegawai.';if(!form.elements.id.value){document.getElementById('passwordCreateWrap').classList.toggle('d-none',managed);form.elements.password.required=!managed}}
function openCreate(){form.reset();form.elements.id.value='';resetIdentity();document.getElementById('identityType').disabled=false;document.getElementById('identitySearch').disabled=false;document.getElementById('identityResult').disabled=false;document.getElementById('btnIdentitySearch').disabled=false;document.getElementById('passwordCreateWrap').classList.remove('d-none');form.elements.password.required=true;setManagedUI(false);modal.show()}
async function openEdit(id){const r=await fetch(`${base}/settings/user?format=json&id=${encodeURIComponent(id)}`,{headers:{Accept:'application/json','X-Requested-With':'XMLHttpRequest'}});const j=await r.json();if(!r.ok||j.status!=='success'){show(j.message||'User tidak dapat dibuka.');return}const u=j.data.user;form.reset();form.elements.id.value=u.id;form.elements.username.value=u.username||'';form.elements.primary_role.value=u.role||'';form.elements.status_aktif.value=String(u.status_aktif??1);document.getElementById('passwordCreateWrap').classList.add('d-none');form.elements.password.required=false;document.querySelectorAll('.secondary-role').forEach(c=>c.checked=(u.secondary_roles||[]).includes(c.value));resetIdentity();let type='',identityId='';if(u.id_guru){type='guru';identityId=u.id_guru}else if(u.id_pegawai){type='pegawai';identityId=u.id_pegawai}else if(u.id_siswa){type='siswa';identityId=u.id_siswa}document.getElementById('identityType').value=type;if(type&&identityId){document.getElementById('identityResult').innerHTML=`<option value="${esc(identityId)}" selected>${esc(u.identity_label||'Identitas')}</option>`;form.elements[`id_${type}`].value=identityId}const managed=!!u.credential_managed;setManagedUI(managed);document.getElementById('identityType').disabled=managed;document.getElementById('identitySearch').disabled=managed;document.getElementById('identityResult').disabled=managed;document.getElementById('btnIdentitySearch').disabled=managed;document.getElementById('identityHelp').textContent=managed?'Relasi Guru/Pegawai dikelola dari Master dan tidak dapat dipindah di halaman ini.':'';modal.show()}
async function searchIdentity(){const type=document.getElementById('identityType').value,search=document.getElementById('identitySearch').value.trim(),sel=document.getElementById('identityResult');resetIdentity();if(!type){setManagedUI(false);return}const p=new URLSearchParams({format:'json',mode:'options',type,search});const r=await fetch(`${base}/settings/user?${p}`,{headers:{Accept:'application/json','X-Requested-With':'XMLHttpRequest'}});const j=await r.json();if(!r.ok||j.status!=='success'){show(j.message||'Identitas gagal dicari.');return}sel.innerHTML='<option value="">Pilih identitas</option>'+((j.data.rows||[]).map(x=>`<option value="${x.id}" data-used="${x.id_user||''}">${esc(x.kode||'-')} — ${esc(x.nama)}${x.id_user?' [sudah terhubung]':''}</option>`).join(''));setManagedUI(type==='guru'||type==='pegawai')}
function applyIdentity(){const type=document.getElementById('identityType').value,value=document.getElementById('identityResult').value;clearIdentity();if(type&&value)form.elements[`id_${type}`].value=value;setManagedUI(type==='guru'||type==='pegawai')}
function bindRows(){document.querySelectorAll('.btn-edit-user').forEach(b=>b.addEventListener('click',()=>openEdit(b.dataset.id)));document.querySelectorAll('.btn-reset-user').forEach(b=>b.addEventListener('click',()=>{resetForm.reset();resetForm.elements.id.value=b.dataset.id;const m=managedById.get(String(b.dataset.id))===true;document.getElementById('managedResetInfo').classList.toggle('d-none',!m);document.getElementById('manualResetFields').classList.toggle('d-none',m);resetForm.elements.password.required=!m;resetForm.elements.password_confirmation.required=!m;resetModal.show()}));document.querySelectorAll('.btn-delete-user').forEach(b=>b.addEventListener('click',async()=>{if(!confirm('Hapus user ini?'))return;const r=await fetch(`${base}/settings/user/delete/${b.dataset.id}`,{method:'DELETE',headers:{Accept:'application/json','X-Requested-With':'XMLHttpRequest'}});const j=await r.json();if(!r.ok||j.status!=='success'){show(j.message||'User gagal dihapus.');return}show(j.message||'User dihapus.','success');load()}))}
document.getElementById('btnUserBaru').addEventListener('click',openCreate);document.getElementById('btnUserCari').addEventListener('click',()=>{offset=0;load()});document.getElementById('userPrev').addEventListener('click',()=>{offset=Math.max(0,offset-limit);load()});document.getElementById('userNext').addEventListener('click',()=>{if(offset+limit<total){offset+=limit;load()}});document.getElementById('btnIdentitySearch').addEventListener('click',searchIdentity);document.getElementById('identityType').addEventListener('change',()=>{document.getElementById('identitySearch').value='';resetIdentity();setManagedUI(['guru','pegawai'].includes(document.getElementById('identityType').value))});document.getElementById('identityResult').addEventListener('change',applyIdentity);
form.addEventListener('submit',async e=>{e.preventDefault();if(!document.getElementById('identityType').disabled)applyIdentity();const fd=new FormData(form),id=String(fd.get('id')||'');fd.delete('id');let r;if(!id){r=await fetch(`${base}/settings/user/create`,{method:'POST',body:fd,headers:{Accept:'application/json','X-Requested-With':'XMLHttpRequest'}})}else{fd.delete('password');const payload={};for(const [k,v] of fd.entries()){if(k==='secondary_roles[]'){payload.secondary_roles??=[];payload.secondary_roles.push(v)}else payload[k]=v}r=await fetch(`${base}/settings/user/update/${id}`,{method:'PUT',body:JSON.stringify(payload),headers:{Accept:'application/json','Content-Type':'application/json','X-Requested-With':'XMLHttpRequest'}})}const j=await r.json();if(!r.ok||j.status!=='success'){show(j.message||'User gagal disimpan.');return}modal.hide();show(j.message||'User berhasil disimpan.','success');load()});
resetForm.addEventListener('submit',async e=>{e.preventDefault();const id=resetForm.elements.id.value;const r=await fetch(`${base}/settings/user/reset/${id}`,{method:'POST',body:new FormData(resetForm),headers:{Accept:'application/json','X-Requested-With':'XMLHttpRequest'}});const j=await r.json();if(!r.ok||j.status!=='success'){show(j.message||'Reset password gagal.');return}resetModal.hide();show(j.message||'Password berhasil direset.','success');load()});
load();
})();
