(() => {
'use strict';
const app=document.getElementById('kartuApp');if(!app)return;
const base=String(app.dataset.baseUrl||'').replace(/\/+$/,'');let offset=0,total=0;const limit=50;const body=document.getElementById('kartuBody');
const esc=v=>{const d=document.createElement('div');d.textContent=v??'';return d.innerHTML};
const params=()=>{const p=new URLSearchParams({format:'json',limit,offset});const s=document.getElementById('kartuSearch')?.value,st=document.getElementById('kartuStatus')?.value;if(s)p.set('search',s);if(st)p.set('status',st);return p};
async function load(){const r=await fetch(`${base}/kartu/daftar?${params()}`,{headers:{Accept:'application/json','X-Requested-With':'XMLHttpRequest'}});const j=await r.json();if(!r.ok||j.status!=='success'){alert(j.message||'Gagal memuat kartu');return}const d=j.data||{};total=Number(d.total||0);body.innerHTML=(d.rows||[]).map(x=>`<tr><td>${esc(x.nisn)}<br><strong>${esc(x.nama)}</strong></td><td>${esc(x.nomor_kartu)}</td><td>${esc(x.tanggal_terbit)}</td><td>${esc(x.status_aktif)}</td><td class="text-end"><a class="btn btn-sm btn-outline-primary" href="${base}/kartu/preview/${x.id}">Preview</a> <a class="btn btn-sm btn-outline-secondary" href="${base}/kartu/download/${x.id}">PDF</a>${d.can_manage?` <button class="btn btn-sm btn-outline-warning btn-reissue" data-id="${x.id}">Reissue</button>`:''}</td></tr>`).join('')||'<tr><td colspan="5" class="text-center text-muted">Belum ada kartu.</td></tr>';document.getElementById('kartuInfo').textContent=`${total?offset+1:0}-${Math.min(offset+limit,total)} dari ${total}`;document.getElementById('kartuPrev').disabled=offset<=0;document.getElementById('kartuNext').disabled=offset+limit>=total;document.querySelectorAll('.btn-reissue').forEach(b=>b.addEventListener('click',async()=>{const r=await fetch(`${base}/kartu/reissue/${b.dataset.id}`,{method:'POST',headers:{Accept:'application/json','X-Requested-With':'XMLHttpRequest'}});const j=await r.json();alert(j.message||'Selesai')}))}
document.getElementById('btnKartuCari')?.addEventListener('click',()=>{offset=0;load()});document.getElementById('kartuPrev')?.addEventListener('click',()=>{offset=Math.max(0,offset-limit);load()});document.getElementById('kartuNext')?.addEventListener('click',()=>{if(offset+limit<total){offset+=limit;load()}});
document.getElementById('formGenerateKartu')?.addEventListener('submit',async e=>{e.preventDefault();const r=await fetch(`${base}/kartu/generate`,{method:'POST',body:new FormData(e.target),headers:{Accept:'application/json','X-Requested-With':'XMLHttpRequest'}});const j=await r.json();if(!r.ok||j.status!=='success'){alert(j.message||'Generate gagal');return}alert(j.message||'Generate selesai');location.reload()});
load();
})();